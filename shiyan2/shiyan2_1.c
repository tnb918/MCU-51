#include<reg51.h>
void int0() interrupt 0 //外部0中断
{
	P1=0x0f;//熄灭发光二极管且对开关输入端先输出1
	P1<<=4;//读入开关状态，左移四位，使开关反映在二极管上
	P1=~P1;//对P1口内容取反
}
main()//主函数
{
	EA=1;//开中断总开关
	EX0=1;//允许外部0中断
	IT0=1;//负跳沿产生中断
	while(1);//等待中断
}